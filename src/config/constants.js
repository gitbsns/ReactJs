export const apiMethods = Object.freeze(["post", "put", "patch", "delete"]);
export const statusCodes = Object.freeze([400, 404, 500]);
export const networkError = Object.freeze("ERR_NETWORK");
export const noInternet = Object.freeze("No internet connection");
export const errMessage = Object.freeze("Something went wrong");
